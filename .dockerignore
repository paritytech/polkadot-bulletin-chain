target/
.git/
node_modules/
docs/
examples/
zombienet/
*.log
*.json
!Cargo.lock
!package.json

# Nothing below is needed to build the stress-test binary. Workspace members
# (pallets/, runtimes/, sdk/rust, zombienet-sdk-tests) must stay: cargo loads
# every member manifest to resolve the workspace, even when building just one
# package.
chainspecs/
console-ui/
scripts/
sdk/typescript/
templates/
utils/
